name: Build & Deploy demo
on: 
  push:
    branches:
      - main
  pull_request:
    types: 
    - opened
    - synchronize
jobs:
  Build:
   runs-on: ubuntu-latest
   if: github.event_name == 'pull_request'
   steps:
     - name: Build step
       run: echo "Running this build"

  deploy:
   runs-on: ubuntu-slim
   if: github.event_name == 'push'
   steps:
     - name: Deploy step
       run: echo "deploy the app"
