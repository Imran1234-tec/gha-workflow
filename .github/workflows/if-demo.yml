name: if condition demo
on: 
  push:
    branches:
     - main
     - feature
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: run this build if main branch
       if: github.ref_name == 'feature'
       run: echo "This is main branch"

