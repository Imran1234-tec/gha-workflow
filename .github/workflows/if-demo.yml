name: if condition demo
on: 
  push:
    branches:
     - main
     - feature
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
     - name: run this build if main branch
       if: github.ref == 'refs/heads/main'
       run: echo "This is main branch"

     - name: run this step
       run: echo "This step is running with condition"
